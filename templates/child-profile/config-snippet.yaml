# Merge into a dedicated child Hermes profile config. Example only.
# The child profile should expose only the narrow child toolset.
platform_toolsets:
  telegram:
    - learnbuddy_child
    - tts
    - vision
# Forbidden for child profile by default:
# - terminal
# - file
# - code_execution
# - homeassistant
# - messaging
# - skills
# - delegation
# - cronjob
