# Data Model

MVP storage is JSONL because it is easy to inspect and recover.

Planned files:

- `settings.json`
- `exercises.jsonl`
- `sessions.jsonl`
- `answers.jsonl`
- `state.json`
- `delivery-events.jsonl`

SQLite migration is planned once dashboard/filtering/multi-child support needs it.
